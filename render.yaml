services:
  - type: web
    name: money-generator-machine
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_app:app --timeout 120 --worker-class gthread --workers 1 --threads 4
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
